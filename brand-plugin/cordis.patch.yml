# dsh-claude-brand bundle patch: inserts its dshClient row into the web roster.
# Install the bundle with `dsh plugin --profile <name> add <path-or-git-url>`.
- insert:
    - id: claude-brand
      name: dsh-claude-brand
