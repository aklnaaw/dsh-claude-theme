# dsh-font-switcher bundle patch: inserts its dshClient row into the web roster.
# Install the bundle with `dsh plugin --profile <name> add <path-or-git-url>`.
- insert:
    - id: font-switcher
      name: dsh-font-switcher
