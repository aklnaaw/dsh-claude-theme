/**
 * Host half of dsh-claude-crab.
 *
 * Empty by design: Clawd is entirely a browser-side presentation concern
 * (one slot occupant plus pointer listeners). No host state, no RPC.
 */
export const name = "dsh-claude-crab";
export const inject = [];
export function apply() {}
